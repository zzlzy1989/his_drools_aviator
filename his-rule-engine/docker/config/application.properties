# Nacos 3.x Configuration
# Standalone mode with embedded storage
nacos.mode=standalone
spring.sql.init.mode=embedded

# Disable authentication for development
nacos.core.auth.enabled=false
nacos.core.auth.plugin.enabled=false

# MySQL Configuration (using external DB)
db.num=1
db.url.0=jdbc:mysql://192.168.1.105:3306/his_nacos?characterEncoding=utf8&useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
db.user.0=root
db.password.0=testhub123

# Memory settings
JVM_XMS=256m
JVM_XMX=512m
