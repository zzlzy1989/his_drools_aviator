# HIS 动态规则中台 — 产品需求文档（PRD）

> 文档版本: v1.0  
> 创建日期: 2026-04-26  
> 文档状态: 草稿  
> 适用项目: his_drools_aviator

---

## 1. 项目概述

### 1.1 项目背景

传统 HIS（医院信息系统）将业务规则（医保报销、质控、合理用药、DRG 分组等）硬编码在核心代码中，导致：

- 规则变更依赖厂商发版，周期长达 2~4 周
- 医院无法自主调整医保政策参数（起付线、报销比例等）
- 新增监管规则需修改核心代码，系统稳定性风险高
- 各医院政策差异大，一套代码难以适配所有场景

### 1.2 项目目标

构建**动态规则中台**，将高频变化的业务规则从 HIS 核心系统中抽离，实现：

| 目标 | 量化指标 |
|------|---------|
| 规则热更新 | 规则变更从"2~4周"缩短至"分钟级" |
| 医院自主维护 | 医保专员可在后台自主维护规则，无需代码修改 |
| 系统稳定性 | 规则引擎与 HIS 底座解耦，规则异常不影响核心事务 |
| 多租户支持 | 一套平台服务多家医院，配置/数据/规则完全隔离 |
| 高性能计算 | 单次结算规则执行 < 10ms |

### 1.3 项目范围

**本期实现（V1.0）**：

- 规则管理服务（规则 CRUD、版本管理、DRL 发布）
- 公式管理服务（公式 CRUD、语法校验、Nacos 同步）
- 医保结算服务（费用结算、报销计算、规则执行）
- 合理用药服务（处方审核、配伍禁忌、极量检查）
- 质控服务（院感防控、质控规则、拦截卡控）
- DRG 分组服务（DRG/DIP 分组、权重计算）
- API 网关（统一入口、路由转发、负载均衡）

**下期规划（V2.0）**：

- 规则可视化编排（拖拽式规则流设计）
- 规则测试沙箱（模拟数据验证规则效果）
- 规则执行监控大屏（实时命中率、耗时统计）
- 规则市场（医院间共享优质规则模板）

---

## 2. 用户角色与权限

### 2.1 角色定义

| 角色 | 标识 | 描述 | 典型用户 |
|------|------|------|---------|
| 系统管理员 | `ADMIN` | 平台运维、租户管理、服务监控 | 医院信息科 |
| 规则管理员 | `RULE_ADMIN` | 规则创建、编辑、发布、停用 | HIS 实施工程师 |
| 医保专员 | `INSURANCE_SPEC` | 医保规则维护、公式调整、参数配置 | 医院医保办 |
| 药剂师 | `PHARMACIST` | 合理用药规则维护、处方审核规则配置 | 医院药剂科 |
| 质控专员 | `QUALITY_SPEC` | 质控规则维护、院感规则配置 | 医院质控科 |
| 医生 | `DOCTOR` | 查看规则拦截结果、处理警告/阻断 | 临床医生 |
| 审计员 | `AUDITOR` | 查看操作日志、规则变更历史、执行记录 | 医院审计科 |

### 2.2 权限矩阵

| 功能模块 | ADMIN | RULE_ADMIN | INSURANCE_SPEC | PHARMACIST | QUALITY_SPEC | DOCTOR | AUDITOR |
|---------|:-----:|:----------:|:--------------:|:----------:|:------------:|:------:|:-------:|
| 租户管理 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 规则 CRUD | ✅ | ✅ | ⚠️ | ❌ | ❌ | ❌ | ❌ |
| 规则发布 | ✅ | ✅ | ⚠️ | ❌ | ❌ | ❌ | ❌ |
| 公式管理 | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 结算执行 | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 用药规则 | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ | ❌ |
| 质控规则 | ✅ | ✅ | ❌ | ❌ | ✅ | ❌ | ❌ |
| 查看拦截结果 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 操作日志 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ |
| 规则回滚 | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |

> ✅ 完全权限 &nbsp; ⚠️ 仅可管理自己负责的规则类别 &nbsp; ❌ 无权限

---

## 3. 功能需求

### 3.1 核心功能

#### 3.1.1 规则管理模块（his-rule-service）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-R01 | 规则列表 | 分页查询规则，支持按类别、状态、关键词筛选 | P0 |
| FR-R02 | 规则创建 | 创建规则定义，填写规则 Key、DRL 内容、分类、描述 | P0 |
| FR-R03 | 规则编辑 | 编辑规则内容，草稿状态可修改，已发布状态需先停用 | P0 |
| FR-R04 | 规则删除 | 软删除规则，级联停用关联实例 | P0 |
| FR-R05 | 规则发布 | 规则语法校验通过后发布生效，自动递增版本号 | P0 |
| FR-R06 | 规则停用 | 停用已生效规则，不影响历史结算数据 | P0 |
| FR-R07 | 规则版本管理 | 查看规则历史版本，支持版本对比和回滚 | P1 |
| FR-R08 | 规则导入/导出 | 批量导入 DRL 文件，导出规则配置 | P1 |
| FR-R09 | 规则校验 | 语法校验、命名冲突检测、依赖公式存在性检查 | P0 |
| FR-R10 | 规则分组 | 按业务域（医保/用药/质控/DRG）分组管理 | P1 |

#### 3.1.2 公式管理模块（his-formula-service）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-F01 | 公式列表 | 分页查询公式，支持按分类、状态筛选 | P0 |
| FR-F02 | 公式创建 | 创建公式定义，填写公式 Key、表达式文本、参数定义 | P0 |
| FR-F03 | 公式编辑 | 编辑公式内容，草稿状态可修改 | P0 |
| FR-F04 | 公式语法校验 | 使用 Aviator 编译验证语法正确性 | P0 |
| FR-F05 | 公式发布 | 发布后同步至 Nacos 配置中心，自动刷新缓存 | P0 |
| FR-F06 | 公式参数管理 | 定义公式参数（名称、类型、默认值、描述） | P0 |
| FR-F07 | 公式测试 | 输入测试参数，验证公式计算结果 | P1 |
| FR-F08 | 公式版本管理 | 查看历史版本，支持版本对比 | P1 |

#### 3.1.3 医保结算模块（his-settlement-service）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-S01 | 结算请求 | 接收结算请求，构建 SettlementFact 事实对象 | P0 |
| FR-S02 | 身份校验 | 校验患者身份类型（职工/居民/新农合等） | P0 |
| FR-S03 | 起付线计算 | 根据患者类型、医院等级确定起付线 | P0 |
| FR-S04 | 报销比例计算 | 根据医保政策确定报销比例 | P0 |
| FR-S05 | 报销金额计算 | 调用 Aviator 公式计算最终报销金额 | P0 |
| FR-S06 | 目录限制校验 | 校验药品/项目是否在医保目录内 | P0 |
| FR-S07 | 封顶线校验 | 校验是否超过年度报销封顶线 | P1 |
| FR-S08 | 结算结果记录 | 保存结算结果（总费用、报销金额、自付金额） | P0 |
| FR-S09 | 结算历史查询 | 查询历史结算记录，支持按患者、时间筛选 | P1 |
| FR-S10 | 重复结算拦截 | 同一次就诊只产生一条有效结算记录 | P0 |

#### 3.1.4 合理用药模块（his-drug-service）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-D01 | 处方审核 | 接收处方数据，触发用药规则检查 | P0 |
| FR-D02 | 配伍禁忌检查 | 检查处方中药品是否存在配伍禁忌 | P0 |
| FR-D03 | 极量检查 | 检查药品用量是否超过极量（调用 Aviator 公式） | P0 |
| FR-D04 | 重复用药检查 | 检查是否存在相同成分/同类药品重复使用 | P1 |
| FR-D05 | 过敏史检查 | 检查患者是否对处方药品过敏 | P0 |
| FR-D06 | 特殊人群用药 | 检查孕妇、儿童、老人等特殊人群用药禁忌 | P1 |
| FR-D07 | 抗生素分级管理 | 检查抗生素使用是否符合分级管理要求 | P1 |
| FR-D08 | 干预结果返回 | 返回 PASS/WARN/BLOCK 三级干预结果 | P0 |

#### 3.1.5 质控模块（his-quality-service）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-Q01 | 院感规则检查 | 检查是否符合院感防控要求 | P0 |
| FR-Q02 | 质控规则执行 | 执行质控规则（如抗菌药物使用率、检查阳性率等） | P0 |
| FR-Q03 | 拦截卡控 | 对违反质控规则的操作进行拦截 | P0 |
| FR-Q04 | 质控指标统计 | 统计质控指标完成情况 | P1 |
| FR-Q05 | 质控报告生成 | 生成质控报告（日/周/月） | P1 |

#### 3.1.6 DRG 分组模块（his-drg-service）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-G01 | DRG 分组 | 根据诊断编码、手术编码进行 DRG 分组 | P0 |
| FR-G02 | 权重计算 | 计算 DRG 权重（调用 Aviator 公式） | P0 |
| FR-G03 | 标准分值计算 | 计算标准分值和预期费用 | P0 |
| FR-G04 | DIP 分组 | 支持 DIP（按病种分值付费）分组 | P1 |
| FR-G05 | 分组结果查询 | 查询历史分组结果 | P1 |

#### 3.1.7 API 网关（his-gateway）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-GW01 | 路由转发 | 根据 URL 路径将请求路由到对应微服务 | P0 |
| FR-GW02 | 负载均衡 | 对同一服务的多个实例进行负载均衡 | P0 |
| FR-GW03 | 统一鉴权 | JWT Token 校验，权限拦截 | P0 |
| FR-GW04 | 限流熔断 | 敏感接口限流，服务异常时熔断降级 | P1 |
| FR-GW05 | 日志记录 | 记录请求日志（请求路径、耗时、状态码） | P0 |
| FR-GW06 | 跨域处理 | CORS 配置，支持前端跨域访问 | P0 |

### 3.2 次要功能

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|:------:|
| FR-M01 | 操作日志 | 记录所有规则/公式的创建、修改、发布操作 | P1 |
| FR-M02 | 审计日志 | 记录规则执行过程、命中情况、干预结果 | P1 |
| FR-M03 | 租户管理 | 多租户隔离，每个医院独立配置 | P1 |
| FR-M04 | 缓存管理 | 查看缓存命中率，手动刷新缓存 | P2 |
| FR-M05 | 健康检查 | 服务健康状态监控 | P1 |
| FR-M06 | 配置中心集成 | Nacos 配置管理，支持动态刷新 | P0 |

---

## 4. 非功能需求

### 4.1 性能需求

| 编号 | 指标 | 目标值 | 测试方法 |
|------|------|--------|---------|
| NFR-P01 | 单条规则匹配耗时 | < 5ms | JMeter 压测 |
| NFR-P02 | Aviator 表达式执行耗时（已缓存） | < 0.5ms | 单元测试 |
| NFR-P03 | 完整结算流程耗时 | < 50ms | 集成测试 |
| NFR-P04 | Nacos 配置变更到生效时间 | < 2s | 手动测试 |
| NFR-P05 | 规则引擎冷启动时间（100 条规则） | < 1s | 启动日志 |
| NFR-P06 | API 网关响应时间（P99） | < 100ms | 压测报告 |
| NFR-P07 | 系统并发支持 | ≥ 500 TPS | JMeter 压测 |
| NFR-P08 | Caffeine 缓存命中率 | ≥ 95% | 监控指标 |

### 4.2 安全需求

| 编号 | 需求 | 描述 |
|------|------|------|
| NFR-S01 | 身份认证 | 所有 API 接口需 JWT Token 认证 |
| NFR-S02 | 权限控制 | 基于 RBAC 的接口级权限控制 |
| NFR-S03 | 租户隔离 | 数据/配置/规则按租户完全隔离 |
| NFR-S04 | SQL 注入防护 | MyBatis 参数化查询，禁止 `${}` 拼接 |
| NFR-S05 | XSS 防护 | 输入输出编码过滤 |
| NFR-S06 | Aviator 表达式注入防护 | 白名单校验，禁用危险函数（System.exit 等） |
| NFR-S07 | DRL 安全拦截 | 禁止 DRL 中包含 Java 代码块 |
| NFR-S08 | 敏感数据脱敏 | 日志中手机号/身份证/银行卡号脱敏 |
| NFR-S09 | 接口限流 | 敏感接口（规则发布/结算执行）限流 |
| NFR-S10 | 传输加密 | HTTPS 传输，敏感配置加密存储 |

### 4.3 可用性需求

| 编号 | 需求 | 描述 |
|------|------|------|
| NFR-A01 | 服务可用性 | ≥ 99.9%（全年停机时间 < 8.76 小时） |
| NFR-A02 | 故障恢复 | 服务异常后自动重启，恢复时间 < 30s |
| NFR-A03 | 降级策略 | 规则引擎异常时降级为 PASS，不阻塞 HIS 核心流程 |
| NFR-A04 | 数据备份 | 数据库每日自动备份，保留 30 天 |

### 4.4 兼容性需求

| 编号 | 需求 | 描述 |
|------|------|------|
| NFR-C01 | JDK 版本 | 支持 JDK 21+ |
| NFR-C02 | 数据库 | MySQL 8.0+ |
| NFR-C03 | 浏览器 | Chrome 90+、Edge 90+、Firefox 88+ |
| NFR-C04 | API 版本 | URL 带版本号（/api/v1/），支持多版本共存 |
| NFR-C05 | 编码格式 | 全局 UTF-8，数据库 utf8mb4 |

### 4.5 可维护性需求

| 编号 | 需求 | 描述 |
|------|------|------|
| NFR-M01 | 日志规范 | 统一日志格式（JSON），5 级日志（ERROR/WARN/INFO/DEBUG/TRACE） |
| NFR-M02 | 链路追踪 | 每个请求携带 traceId，便于问题定位 |
| NFR-M03 | 监控指标 | 暴露 Prometheus 指标（请求量、耗时、错误率） |
| NFR-M04 | 代码规范 | 遵循阿里巴巴 Java 开发手册 |
| NFR-M05 | 测试覆盖率 | 核心规则 ≥ 90%，工具类 ≥ 80%，Controller ≥ 70% |

---

## 5. 业务流程

### 5.1 医保结算流程

```
[ HIS 结算请求 ]
       │
       ▼
[ API 网关 ] ── JWT 校验 ── 路由到 Settlement Service
       │
       ▼
[ 构建 SettlementFact ]
       │
       ├── 患者身份校验 ──────────────────────┐
       │                                      ▼
       │                              [ 身份缺失? ] ── 是 ──→ [ 返回 BLOCK ]
       │                                      │
       │                                     否
       │                                      ▼
       │                              [ 确定起付线 ]
       │                                      │
       │                                      ▼
       │                              [ 确定报销比例 ]
       │                                      │
       │                                      ▼
       │                              [ 目录限制校验 ] ── 不在目录 ──→ [ 返回 WARN/BLOCK ]
       │                                      │
       │                                     通过
       │                                      ▼
       │                              [ 调用 Aviator 公式 ]
       │                                      │
       │                                      ▼
       │                              [ 计算报销金额 ]
       │                                      │
       │                                      ▼
       │                              [ 封顶线校验 ] ── 超限 ──→ [ 调整金额 ]
       │                                      │
       │                                     通过
       │                                      ▼
       │                              [ 保存结算结果 ]
       │                                      │
       │                                      ▼
       └────────────────────────────── [ 返回结算结果 ]
```

### 5.2 合理用药审核流程

```
[ 医生站保存处方 ]
       │
       ▼
[ HIS 抛出 EVENT_DRUG_PRESCRIBE 事件 ]
       │
       ▼
[ API 网关 ] ── 路由到 Drug Service
       │
       ▼
[ Skill Gateway 匹配关注的 Skill ]
       │
       ├── [ 医保限制 Skill ] ── 检查药品是否在医保目录
       │
       ├── [ 合理用药 Skill ] ── 执行以下检查：
       │       ├── 配伍禁忌检查
       │       ├── 极量检查（调用 Aviator 公式）
       │       ├── 重复用药检查
       │       ├── 过敏史检查
       │       └── 特殊人群用药检查
       │
       └── [ 院感拦截 Skill ] ── 检查院感防控要求
              │
              ▼
       [ 汇总执行结果 ]
              │
              ├── 全部 PASS ──→ [ 放行，处方保存成功 ]
              ├── 存在 WARN ──→ [ 弹窗警告，医生可强制双签名通过 ]
              └── 存在 BLOCK ──→ [ 阻断，处方保存失败，返回原因 ]
```

### 5.3 规则发布流程

```
[ 规则管理员创建/编辑规则 ]
       │
       ▼
[ 草稿状态（draft） ]
       │
       ▼
[ 提交校验 ]
       │
       ├── 语法校验 ── 失败 ──→ [ 返回错误信息 ]
       │
       ├── 命名冲突检测 ── 冲突 ──→ [ 返回冲突信息 ]
       │
       └── 依赖检查 ── 缺失 ──→ [ 返回缺失依赖 ]
              │
             通过
              ▼
[ 已校验状态（validated） ]
       │
       ▼
[ 发布生效 ]
       │
       ├── 更新规则状态为 active
       ├── 递增版本号
       ├── 同步至 Nacos（如含公式引用）
       ├── 刷新 Caffeine 缓存
       └── 记录操作日志
              │
              ▼
       [ 生效状态（active） ]
              │
              ▼
       [ 规则引擎自动加载新规则 ]
```

### 5.4 规则状态机

```
                    ┌──────────┐
                    │  草稿     │
                    │ (draft)  │
                    └────┬─────┘
                         │ 提交校验
                         ▼
                    ┌──────────┐
                    │ 已校验    │
                    │(validated)│
                    └────┬─────┘
                         │ 发布
                         ▼
                    ┌──────────┐
              ┌─────│  生效     │─────┐
              │     │ (active)  │     │
              │     └────┬──────┘     │
              │        停用           │ 编辑（先停用）
              │        ▼              │
              │  ┌──────────┐         │
              │  │  停用     │─────────┘
              │  │(inactive) │
              │  └──────────┘
              │
              ▼
         ┌──────────┐
         │  已删除   │
         │ (deleted) │
         └──────────┘
```

---

## 6. 数据需求

### 6.1 核心数据表

#### 6.1.1 规则定义表（rule_definition）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| tenant_id | VARCHAR(64) | NOT NULL, INDEX | 租户 ID |
| rule_key | VARCHAR(128) | NOT NULL, UNIQUE(tenant_id) | 规则唯一标识 |
| rule_text | TEXT | NOT NULL | DRL 规则内容 |
| category | VARCHAR(32) | NOT NULL | 分类（reimbursement/drug/quality/drg） |
| version | INT | DEFAULT 1 | 版本号 |
| status | VARCHAR(16) | DEFAULT 'draft' | 状态（draft/validated/active/inactive） |
| description | VARCHAR(500) | | 规则描述 |
| created_by | VARCHAR(64) | | 创建人 |
| created_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_by | VARCHAR(64) | | 更新人 |
| updated_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

#### 6.1.2 公式定义表（aviator_formula）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| tenant_id | VARCHAR(64) | NOT NULL, INDEX | 租户 ID |
| formula_key | VARCHAR(128) | NOT NULL, UNIQUE(tenant_id) | 公式唯一标识 |
| formula_text | VARCHAR(1000) | NOT NULL | 表达式文本 |
| category | VARCHAR(32) | NOT NULL | 分类 |
| version | INT | DEFAULT 1 | 版本号 |
| status | VARCHAR(16) | DEFAULT 'draft' | 状态 |
| description | VARCHAR(500) | | 公式描述 |
| created_by | VARCHAR(64) | | 创建人 |
| created_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_by | VARCHAR(64) | | 更新人 |
| updated_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

#### 6.1.3 公式参数表（formula_param）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| formula_id | BIGINT | NOT NULL, FK → aviator_formula.id | 所属公式 |
| param_name | VARCHAR(64) | NOT NULL | 参数名 |
| param_type | VARCHAR(32) | NOT NULL | 参数类型（BigDecimal/String/Integer） |
| default_value | VARCHAR(256) | | 默认值 |
| description | VARCHAR(256) | | 参数描述 |

#### 6.1.4 结算结果表（settlement_result）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| tenant_id | VARCHAR(64) | NOT NULL, INDEX | 租户 ID |
| settlement_id | VARCHAR(64) | NOT NULL, UNIQUE | 结算流水号 |
| visit_id | VARCHAR(64) | NOT NULL, UNIQUE(tenant_id) | 就诊 ID |
| patient_id | VARCHAR(64) | NOT NULL | 患者 ID |
| patient_type | VARCHAR(32) | NOT NULL | 患者类型 |
| total_fee | DECIMAL(12,2) | NOT NULL | 总费用 |
| deductible | DECIMAL(12,2) | | 起付线 |
| reimburse_amount | DECIMAL(12,2) | | 报销金额 |
| self_pay_amount | DECIMAL(12,2) | | 自付金额 |
| status | VARCHAR(16) | DEFAULT 'pending' | 状态 |
| created_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 6.1.5 审计日志表（audit_log）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| tenant_id | VARCHAR(64) | NOT NULL, INDEX | 租户 ID |
| action | VARCHAR(32) | NOT NULL | 操作类型（CREATE/UPDATE/PUBLISH/DELETE/EXECUTE） |
| target_type | VARCHAR(32) | NOT NULL | 目标类型（RULE/FORMULA/SETTLEMENT） |
| target_id | VARCHAR(64) | NOT NULL | 目标 ID |
| operator | VARCHAR(64) | NOT NULL | 操作人 |
| detail | TEXT | | 操作详情（JSON） |
| created_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 操作时间 |

### 6.2 数据量预估

| 表 | 初期数据量 | 年增长 | 保留策略 |
|----|-----------|--------|---------|
| rule_definition | 500 条 | 200 条 | 永久保留 |
| aviator_formula | 300 条 | 100 条 | 永久保留 |
| settlement_result | 10 万条 | 50 万条 | 保留 3 年，归档历史库 |
| audit_log | 50 万条 | 200 万条 | 保留 1 年，归档历史库 |

---

## 7. 界面原型说明

> 以下为管理后台界面原型说明，具体 UI 设计以设计稿为准。

### 7.1 规则管理页面

**页面布局**：

```
┌─────────────────────────────────────────────────────────────┐
│  规则管理                                    [新建规则] [导入] │
├─────────────────────────────────────────────────────────────┤
│  筛选: [分类 ▼] [状态 ▼] [关键词搜索____________] [查询] [重置] │
├─────────────────────────────────────────────────────────────┤
│  规则Key          │ 分类   │ 版本 │ 状态    │ 更新时间   │ 操作  │
│  ─────────────────┼────────┼──────┼─────────┼────────────┼──────│
│  rule.insurance...│ 医保报销│ v3   │ 生效    │ 2026-04-26 │ 编辑 │
│                   │        │      │         │            │ 停用 │
│                   │        │      │         │            │ 历史 │
│  ─────────────────┼────────┼──────┼─────────┼────────────┼──────│
│  rule.drug.max... │ 合理用药│ v1   │ 草稿    │ 2026-04-25 │ 编辑 │
│                   │        │      │         │            │ 删除 │
├─────────────────────────────────────────────────────────────┤
│  共 128 条记录                              < 1 2 3 ... 10 > │
└─────────────────────────────────────────────────────────────┘
```

**新建/编辑规则弹窗**：

```
┌─────────────────────────────────────────────────┐
│  新建规则                                        │
├─────────────────────────────────────────────────┤
│  规则标识: [rule.insurance.resident_reimburse____]│
│  规则分类: [医保报销 ▼]                           │
│  规则描述: [居民医保住院报销规则_________________]│
│                                                 │
│  DRL 内容:                                      │
│  ┌───────────────────────────────────────────┐  │
│  │ package com.his.rules.reimbursement;      │  │
│  │                                           │  │
│  │ import com.his.common.SettlementFact;     │  │
│  │                                           │  │
│  │ rule "居民起付线"                          │  │
│  │     when                                  │  │
│  │         $f: SettlementFact(...)           │  │
│  │     then                                  │  │
│  │         ...                               │  │
│  │ end                                       │  │
│  │                                           │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│              [取消]    [校验]    [保存草稿]       │
└─────────────────────────────────────────────────┘
```

### 7.2 公式管理页面

**页面布局**：

```
┌─────────────────────────────────────────────────────────────┐
│  公式管理                                    [新建公式]       │
├─────────────────────────────────────────────────────────────┤
│  筛选: [分类 ▼] [状态 ▼] [关键词搜索____________] [查询] [重置] │
├─────────────────────────────────────────────────────────────┤
│  公式标识          │ 分类   │ 版本 │ 状态    │ 更新时间   │ 操作  │
│  ─────────────────┼────────┼──────┼─────────┼────────────┼──────│
│  formula.reimb... │ 医保报销│ v5   │ 生效    │ 2026-04-26 │ 编辑 │
│  表达式: round(...│        │      │         │            │ 测试 │
│                   │        │      │         │            │ 历史 │
├─────────────────────────────────────────────────────────────┤
│  共 86 条记录                               < 1 2 3 ... 5 > │
└─────────────────────────────────────────────────────────────┘
```

**公式测试弹窗**：

```
┌─────────────────────────────────────────────────┐
│  公式测试                                        │
├─────────────────────────────────────────────────┤
│  公式: round((totalFee - deductible) * ratio, 2) │
│                                                 │
│  参数输入:                                       │
│  ┌────────────────┬──────────────┬────────────┐ │
│  │ 参数名         │ 类型         │ 测试值     │ │
│  ├────────────────┼──────────────┼────────────┤ │
│  │ totalFee       │ BigDecimal   │ [2000     ]│ │
│  │ deductible     │ BigDecimal   │ [500      ]│ │
│  │ ratio          │ BigDecimal   │ [0.65     ]│ │
│  └────────────────┴──────────────┴────────────┘ │
│                                                 │
│              [执行测试]                          │
│                                                 │
│  测试结果: 975.00                               │
│              [关闭]                              │
└─────────────────────────────────────────────────┘
```

### 7.3 结算执行页面

```
┌─────────────────────────────────────────────────────────────┐
│  医保结算                                                    │
├─────────────────────────────────────────────────────────────┤
│  患者信息:                                                   │
│  姓名: [张三]  患者类型: [居民医保 ▼]  就诊号: [V20260426001] │
│                                                            
│  费用明细:                                                   │
│  ┌────────────────┬──────────┬──────────┬──────────────────┐│
│  │ 项目名称       │ 单价     │ 数量     │ 是否在医保目录   ││
│  ├────────────────┼──────────┼──────────┼──────────────────┤│
│  │ 阿莫西林胶囊   │ 12.50    │ 2        │ ✅               ││
│  │ CT检查         │ 280.00   │ 1        │ ✅               ││
│  │ 自费项目A      │ 150.00   │ 1        │ ❌               ││
│  └────────────────┴──────────┴──────────┴──────────────────┘│
│                                                            
│  结算结果:                                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 总费用: 605.00        起付线: 500.00                  │  │
│  │ 报销比例: 65%         报销金额: 68.25                 │  │
│  │ 自付金额: 536.75                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                            
│                    [执行结算]                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 验收标准

### 8.1 功能验收

| 编号 | 验收项 | 验收方法 | 通过标准 |
|------|--------|---------|---------|
| AC-F01 | 规则 CRUD | 手动操作 | 增删改查功能正常，状态流转正确 |
| AC-F02 | 规则发布 | 发布后调用 | 规则立即生效，无需重启服务 |
| AC-F03 | 公式语法校验 | 输入错误公式 | 正确识别语法错误并提示 |
| AC-F04 | 公式计算 | 输入测试参数 | 计算结果与预期一致，精度正确 |
| AC-F05 | 医保结算 | 模拟结算请求 | 报销金额计算正确，结果保存 |
| AC-F06 | 合理用药审核 | 提交含禁忌处方 | 正确返回 WARN/BLOCK 干预结果 |
| AC-F07 | DRG 分组 | 输入诊断编码 | 正确分组，权重计算准确 |
| AC-F08 | 多租户隔离 | 切换租户访问 | 只能看到本租户数据 |

### 8.2 性能验收

| 编号 | 验收项 | 验收方法 | 通过标准 |
|------|--------|---------|---------|
| AC-P01 | 结算耗时 | JMeter 压测 1000 次 | P99 < 50ms |
| AC-P02 | 并发支持 | JMeter 并发 500 用户 | 错误率 < 0.1% |
| AC-P03 | 缓存命中率 | 查看监控指标 | 命中率 ≥ 95% |
| AC-P04 | 配置刷新 | 修改 Nacos 配置 | 2 秒内生效 |

### 8.3 安全验收

| 编号 | 验收项 | 验收方法 | 通过标准 |
|------|--------|---------|---------|
| AC-S01 | SQL 注入 | 输入注入语句 | 被正确拦截 |
| AC-S02 | Aviator 注入 | 输入 System.exit() | 被白名单拦截 |
| AC-S03 | 权限控制 | 无权限用户访问 | 返回 403 |
| AC-S04 | 日志脱敏 | 查看日志文件 | 敏感信息已脱敏 |

---

## 9. 项目风险与应对措施

### 9.1 技术风险

| 编号 | 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|:----:|---------|
| TR-01 | Drools 规则编译失败导致服务不可用 | 高 | 中 | 规则发布前校验，失败则拒绝发布；保留上一版本快照支持回滚 |
| TR-02 | Aviator 公式语法错误导致运行时异常 | 高 | 中 | 公式保存前强制语法校验；执行时捕获异常并降级 |
| TR-03 | Nacos 配置中心不可用 | 高 | 低 | 本地缓存兜底，配置变更失败不影响已加载规则 |
| TR-04 | Caffeine 缓存内存溢出 | 中 | 低 | 设置合理的 maximumSize 和过期时间；监控内存使用 |
| TR-05 | 微服务间调用延迟 | 中 | 中 | 服务同机房部署；使用 OpenFeign 连接池；设置合理超时 |

### 9.2 业务风险

| 编号 | 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|:----:|---------|
| BR-01 | 医保政策频繁变更导致规则维护压力大 | 高 | 高 | 提供规则模板库；支持规则批量导入；简化配置流程 |
| BR-02 | 医院操作人员不熟悉规则配置 | 高 | 中 | 提供操作手册和培训；公式提供测试功能；规则提供预览 |
| BR-03 | 规则冲突导致结算结果异常 | 高 | 中 | 规则发布前进行冲突检测；提供规则测试沙箱 |
| BR-04 | 多医院政策差异大，规则难以通用 | 中 | 高 | 完善的租户隔离机制；支持医院自定义规则 |

### 9.3 运维风险

| 编号 | 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|:----:|---------|
| OR-01 | 服务部署复杂，运维成本高 | 中 | 中 | 提供 Docker 镜像和 docker-compose 一键部署 |
| OR-02 | 日志量大，存储压力大 | 中 | 中 | 日志分级存储；审计日志定期归档；设置保留策略 |
| OR-03 | 数据库性能瓶颈 | 高 | 低 | 合理设计索引；大表分库分表；读写分离 |

---

## 10. 附录

### 10.1 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| HIS | Hospital Information System | 医院信息系统 |
| DRL | Drools Rule Language | Drools 规则语言 |
| Fact | Fact | 传递给规则引擎的事实对象 |
| KIE | Knowledge Is Everything | Drools 的知识库体系 |
| Aviator | Aviator | 高性能表达式引擎 |
| DRG | Diagnosis Related Groups | 疾病诊断相关分组 |
| DIP | Diagnosis-Intervention Packet | 按病种分值付费 |
| Nacos | Naming and Configuration Service | 阿里巴巴配置中心 |
| Caffeine | Caffeine | Java 高性能缓存库 |
| Skill | Skill | 能力单元，实现特定业务逻辑的组件 |
| Tenant | Tenant | 租户，指使用本平台的一家医院 |

### 10.2 参考文档

- [Spring Cloud Alibaba 官方文档](https://sca.aliyun.com/docs/)
- [Drools 用户手册](https://docs.drools.org/)
- [Aviator 表达式引擎](https://github.com/killme2008/aviator)
- [Nacos 配置管理](https://nacos.io/zh-cn/docs/configuration-management.html)
- [项目架构文档](drools_aviator.md)
- [Skill/Agent 架构设计](test-his-ui-drug.md)

### 10.3 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|---------|--------|
| v1.0 | 2026-04-26 | 初始版本，包含完整需求定义 | AI 助手 |

---

*文档结束*
